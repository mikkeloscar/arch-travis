sudo: required

# env:
#   - ARCH_TRAVIS_VERBOSE=1

arch:
  pacman:
    - procps-ng # required by screenfetch
    - screenfetch
  aur:
    - archey3-git
  script:
    - "sudo archey3"
    - "sudo screenfetch"

script:
  - "curl -s https://raw.githubusercontent.com/mikkeloscar/arch-travis/master/arch-travis.sh | bash"
